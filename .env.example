# Copy this file to .env.local and fill in the values below.
# The first two are required; the last two are optional (AI drafting).

# The single password you type on the /login screen.
# It is ALSO used as the secret that signs your login cookie — so if you
# change it later, everyone gets logged out (which is what you want).
# Pick something long and random.
DASHBOARD_PASSWORD=change-me-to-a-long-random-password

# A GitHub fine-grained Personal Access Token (PAT) with READ + WRITE access
# to the ApagPlayz/content-generation-platform repository. It needs the
# Contents, Issues, Pull requests, and Actions permissions.
# Create one at: https://github.com/settings/tokens?type=beta
GITHUB_TOKEN=github_pat_xxxxxxxxxxxxxxxxxxxxx

# OPTIONAL — where your local project folders live, for the "start from a
# local folder" option in Add a project. Defaults to your Claude Projects
# folder. This feature only works when the dashboard runs on your own Mac.
# CLAUDE_PROJECTS_DIR=/Users/alessiopagliarulo/Documents/Claude Projects

# --- AI drafting ("Draft with AI" on the Process Map) --------------------
# When the dashboard runs on your Mac, AI drafting works out of the box, FOR
# FREE, through the Claude app you're already logged into (your Claude
# subscription) — nothing to configure.
#
# OPTIONAL — only needed if the dashboard runs in the cloud (e.g. Vercel),
# where there's no Claude app. A paid Anthropic API key:
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

# OPTIONAL — which engine to use: "auto" (default: your Claude app if
# available, otherwise the API key), "cli" (Claude app only), or "api".
# DASHBOARD_AI_BACKEND=auto

# OPTIONAL — which Claude model the AI drafting uses. Leave this alone unless
# you have a reason to change it; the default is a good, fast choice.
# DASHBOARD_AI_MODEL=sonnet
